<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox (required for App Store, optional for direct distribution) -->
    <!-- Note: Sandbox is disabled for direct distribution to allow full Photos access -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Hardened Runtime (required for notarization) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>

    <!-- File System Access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Photos Library Access -->
    <key>com.apple.security.personal-information.photos-library</key>
    <true/>

    <!-- AppleEvents (for Photos app integration) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Network Access (for auto-update checks) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Hardware Acceleration (VideoToolbox) -->
    <key>com.apple.security.device.audio-input</key>
    <false/>
    <key>com.apple.security.device.camera</key>
    <false/>
</dict>
</plist>
